import { SearchfilteremployePipe } from './searchfilteremploye.pipe';

describe('SearchfilteremployePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilteremployePipe();
    expect(pipe).toBeTruthy();
  });
});
